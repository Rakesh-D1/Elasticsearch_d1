curl -XGET "localhost:9200/_cat/indices"

curl -XGET "localhost:9200/_cat/indices?v"

 curl -XGET "localhost:9200/_cat/indices?h=index"

 curl -X GET "localhost:9200/_cat/indices?h=index"

 curl -XGET "localhost:9200/series/_mapping?pretty"

 curl "localhost:9200/series/_mapping?pretty" > index-series-mapping.json

 curl -XGET "localhost:9200/series/_mapping?pretty" > index-series-mapping.json

 curl -XPUT "localhost:9200/my-index" -d @index-series-mapping.json

 curl -XPUT "localhost:9200/my-index" @index-series-mapping.json

 curl -XPUT 127.0.0.1:9200/series -d '
{
    "mappings" : {
        "properties" : {
            "film_to_franchise" : {
                "type" : "join",
                "relations" : {"franchise" : "film"}
            } 
        }
    } 
}
'
/usr/bin/curl  -H "Content-Type: application/x-ndjson" -XPUT 127.0.0.1:9200/_bulk?pretty --data-binary @series.json

curl -XGET 127.0.0.1:9200/series/_search?pretty -d '
{
    "query" : {
        "has_parent" : {
            "parent_type" : "franchise" ,
            "query" : {
                "match" : {
                    "title" : "Star Wars"
                }
            }
        }
    }
}
'

curl -XGET 127.0.0.1:9200/series/_search?pretty -d '
{
    "query" : {
        "has_parent" : {
            "parent_type" : "franchise" ,
            "query" : {
                "match" : {
                    "title" : "star wars"
                }
            }
        }
    }
}
'

curl -XGET 127.0.0.1:9200/series/_search?pretty -d '
{
    "query" : {
        "has_parent" : {
            "parent_type" : "franchise" ,
            "query" : {
                "match" : {
                    "title" : "star"
                }
            }
        }
    }
}
'

--------------------------HAS CHILD--------------------------------

curl -XGET 127.0.0.1:9200/series/_search?pretty -d '
{
    "query" : {
        "has_child": {
            "type": "film",
            "query": {
                "match": {
                    "title": "The Force Awakens"
                }
            }
        }
    }
}
'


curl -XGET 127.0.0.1:9200/series/_search?pretty -d '
{
    "query" : {
        "has_child": {
            "type": "film",
            "query": {
                "match": {
                    "title": "test data"
                }
            }
        }
    }
}
'



curl -XGET 127.0.0.1:9200/series/_search?pretty -d '
{
    "query" : {
        "has_child": {
            "type": "film",
            "query": {
                "match": {
                    "title": "test data"
                }
            }
        }
    }
}
'